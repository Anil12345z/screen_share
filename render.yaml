services:
  - name: fullstack-app
    type: web
    env: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm run start
    envVars:
      - key: PORT
        value: 8008
    autoDeploy: true
